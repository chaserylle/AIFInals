
//@keilly
//should minimize and maximize players
public class Move {
	int MINIMUM = "";
	int MAXIMUM = "";
	int minimax(int score[], int node, int column, int row, boolean isMax, boolean isMin)
}
	if(isMaximum){
		int status = MIN;
		//code to be inserted
	}
	else {
		int status = MAX;
	}
}
